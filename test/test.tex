\documentclass{article}

\usepackage[cm]{fullpage}

\usepackage{graphicx}
\usepackage{color}
\usepackage{fixltx2e}
\usepackage{caption}

\author{}
\title{}
\date{}

\newcommand{\executeiffilenewer}[3]{%
    \ifnum\pdfstrcmp
    {\pdffilemoddate{#1}}%
    {\pdffilemoddate{#2}}
    >0%
    {\immediate\write18{#3}}
    \fi%
}
\newcommand{\includesvg}[1]{%
    \executeiffilenewer{#1.svg}{#1.pdf}%
    {inkscape -z -D --file=#1.svg %
    --export-pdf=#1.pdf --export-latex}%
    \input{#1.pdf_tex}%
}

\begin{document}

\pagenumbering{gobble}

\begin{figure}[ht!]
    \centering
    \def\Sa{\(c\)}
    \def\Sb{\((a+b)n_1\)}
    \def\Sc{\((a+b)n_2\)}
    \def\Sd{\((a+b)n_{N-1}\)}
    \def\Se{\(dn_N\)}
    \def\Sf{\(S_Nn_N\)}
    \def\Sg{\(S_{N-1}n_{N-1}\)}
    \def\Sh{\(S_3n_3\)}
    \def\Si{\(S_2n_2\)}
    \def\Sj{\(S_1n_1\)}
    \def\Sk{\(an_N\)}
    \def\Sl{\(an_3\)}
    \def\Sm{\(an_2\)}
    \def\Sn{}%\(i=1\)}
    \def\So{}%\(2\)}
    \def\Sp{}%\(3\)}
    \def\Sq{}%\(N-1\)}
    \def\Sr{}%\(N\)}
    \def\Ss{\(N\)}
    \def\svgwidth{\textwidth}
    \includesvg{urns_fancy}
    \caption*{Diagram of an individual-based model of biased transport with sinks, where \(a,b,c\) and \(d\) are the rates of hopping left, hopping right, inflow and outflow, respectively; the \(S_i\) are the rates of the sinks; and \(n_i\) is the number of particles in the \(i\)-th urn. There is an infinite inflow reservoir and particles cannot return after outflow}
\end{figure}
\end{document}
